seed: 2026
device: cuda

input:
  image: ../ACDC/database/testing/patient107/patient107_4d.nii.gz
  time_index:
  output_dir: outputs/cinema_jacobian

cunsure:
  checkpoint: runs/cunsure_monai3d/best.pt

foundation:
  name: cinema
  repo_path: ../CineMA
  view: sax
  pool: cls
  cache_dir:

jacobian:
  chunk_size: 8
  save_jacobian: true
