seed: 2026
device: auto

input:
  # Frames are already ROI-cropped and percentile-normalized by prepare_hdf5.py.
  h5: processed/val_cunsure_roi.h5
  h5_key: y
  start_index: 0
  limit:
  resume: true
  output_dir: runs/selected/cinema_score_cunsure_roi_all_datasets

score:
  checkpoint: runs/cunsure_score_monai3d_roi/best.pt

foundation:
  name: cinema
  repo_path: ../CineMA
  view: sax
  pool: cls
  cache_dir:

noise_estimation:
  kernel_size: 3
  spectral_floor: 0.000001
  relative_spectral_floor: 0.01
  covariance_floor: 0.0
  save_diagnostics: false

propagation:
  # Direct n ~ N(0, Sigma_k) finite differences; no full Jacobian is formed.
  num_samples: 32
  sample_batch_size: 8
  tau: 0.01
  difference_scheme: forward
  covariance_storage: diag
